非常感谢您的反馈！您提出的两点非常关键：
 * 右上角菜单消失/导航难用：这是因为我上一版 CSS 隐藏了 stHeader（Streamlit 顶部默认栏），导致您无法访问设置（Settings）或查看运行状态。我必须把这个还原回来，并优化侧边栏的使用体验。
 * 海量模板的管理难题：当模板从 10 个变成 100 个、1000 个时，单纯的“卡片平铺”确实会变成灾难。我们需要引入**“多维分类”（Type + Category）和“列表/卡片视图切换”**。
针对您的“大容量管理”需求，我为您重新设计了交互逻辑：
🛠️ v2.5 改进重点
 * 双层分类体系：引入 Type (技术栈：Linux/SQL/PowerShell) 和 Category (业务场景：巡检/备份/清理) 两个维度。
 * 视图切换：增加**“列表模式 (List View)”，像 Excel 一样紧凑展示海量模板；保留“网格模式 (Grid View)”**用于直观查找。
 * 侧边栏回归：利用 Streamlit 原生侧边栏做全局筛选，它永远停靠在左侧，不会“收回去就找不到”。
 * UI 修复：恢复右上角原生菜单，优化深色模式配色。
请直接覆盖 modules/cmd_generator/app.py。
import streamlit as st
import pandas as pd
from jinja2 import Template
import shlex
import uuid
import time

# ==========================================
# 🎨 0. 视觉增强 (修复版)
# ==========================================
def inject_custom_css():
    st.markdown("""
        <style>
        /* 全局字体优化 */
        .stApp { font-family: 'Inter', sans-serif; }
        
        /* 修复：不隐藏顶部 Header，方便你找设置 */
        /* header[data-testid="stHeader"] { visibility: hidden; } */
        
        /* 优化 Expander 样式，让分类更清晰 */
        .streamlit-expanderHeader {
            background-color: #262730;
            border-radius: 4px;
        }
        
        /* 终端窗口样式 */
        .terminal-window {
            background-color: #1e1e1e;
            border-radius: 6px;
            border: 1px solid #333;
            padding: 15px;
            font-family: 'Fira Code', monospace;
            color: #d4d4d4;
            margin-top: 10px;
        }
        
        /* 状态标签样式 */
        .badge-linux { background-color: #f39c12; color: white; padding: 2px 6px; border-radius: 4px; font-size: 0.8em; }
        .badge-sql { background-color: #3498db; color: white; padding: 2px 6px; border-radius: 4px; font-size: 0.8em; }
        .badge-ps { background-color: #2ecc71; color: white; padding: 2px 6px; border-radius: 4px; font-size: 0.8em; }
        </style>
    """, unsafe_allow_html=True)

# ==========================================
# 🧠 1. 核心渲染入口
# ==========================================
def render(cfg_mgr):
    inject_custom_css()
    
    # 获取数据
    templates = cfg_mgr.get_all_templates()
    
    # 状态初始化
    if 'view_state' not in st.session_state: st.session_state['view_state'] = 'DASHBOARD' 
    if 'active_tid' not in st.session_state: st.session_state['active_tid'] = None
    
    # 路由分发
    if st.session_state['view_state'] == 'DASHBOARD':
        render_dashboard(cfg_mgr, templates)
    elif st.session_state['view_state'] == 'WORKBENCH':
        render_workbench(cfg_mgr, templates)
    elif st.session_state['view_state'] == 'EDITOR':
        render_editor(cfg_mgr, templates)

# ==========================================
# 🏠 2. 大厅 (Dashboard) - 高级筛选与管理
# ==========================================
def render_dashboard(cfg_mgr, templates):
    # --- A. 侧边栏：全局筛选器 (永远可见) ---
    with st.sidebar:
        st.header("🗂️ 模板仓库")
        
        # 1. 顶部操作
        if st.button("➕ 新建模板", type="primary", use_container_width=True):
            st.session_state['active_tid'] = "NEW"
            st.session_state['view_state'] = 'EDITOR'
            st.rerun()
            
        st.markdown("---")
        
        # 2. 核心过滤器
        filter_search = st.text_input("🔍 搜索", placeholder="关键词...")
        
        # 自动提取所有 Type 和 Category
        all_types = sorted(list(set(t.get('type', 'Other') for t in templates.values())))
        all_cats = sorted(list(set(t.get('category', '未分类') for t in templates.values())))
        
        filter_type = st.multiselect("技术类型 (Type)", options=all_types, default=all_types)
        filter_cat = st.multiselect("业务场景 (Category)", options=all_cats)
        
        st.markdown("---")
        # 视图切换开关
        view_mode = st.radio("展示模式", ["🪟 网格卡片", "📋 紧凑列表"], index=0)

    # --- B. 主区域：内容展示 ---
    st.title("🚀 Ops Command Center")
    
    # 1. 数据过滤逻辑
    filtered_list = []
    for tid, t in templates.items():
        # 搜索过滤
        term = filter_search.lower()
        if term and (term not in t['name'].lower() and term not in t.get('desc','').lower()):
            continue
        # 类型过滤
        if t.get('type', 'Other') not in filter_type:
            continue
        # 分类过滤 (如果未选则显示所有)
        if filter_cat and t.get('category', '未分类') not in filter_cat:
            continue
            
        t['_id'] = tid
        filtered_list.append(t)
        
    if not filtered_list:
        st.info("没有找到匹配的模板，请调整左侧筛选条件。")
        return

    st.caption(f"共找到 {len(filtered_list)} 个模板")

    # 2. 渲染 (根据选择的视图模式)
    if view_mode == "🪟 网格卡片":
        render_grid_view(filtered_list)
    else:
        render_list_view(filtered_list)

def render_grid_view(items):
    """网格视图：适合浏览，视觉性强"""
    # 按 Type 分组展示，避免杂乱
    grouped_types = sorted(list(set(i.get('type', 'Other') for i in items)))
    
    for g_type in grouped_types:
        st.subheader(f"{get_type_icon(g_type)} {g_type}")
        type_items = [i for i in items if i.get('type', 'Other') == g_type]
        
        cols = st.columns(3)
        for idx, item in enumerate(type_items):
            with cols[idx % 3]:
                with st.container(border=True):
                    # 标题行
                    c1, c2 = st.columns([5, 1])
                    c1.markdown(f"**{item['name']}**")
                    if item.get('source') == 'team':
                        c2.caption("🏢")
                    else:
                        c2.caption("👤")
                    
                    st.caption(f"📂 {item.get('category', '未分类')}")
                    st.text(item.get('desc', '无描述')[:30] + "...")
                    
                    if st.button("运行", key=f"grid_run_{item['_id']}", use_container_width=True):
                        st.session_state['active_tid'] = item['_id']
                        st.session_state['view_state'] = 'WORKBENCH'
                        st.rerun()
                        
                    if st.button("编辑", key=f"grid_edit_{item['_id']}", use_container_width=True):
                        st.session_state['active_tid'] = item['_id']
                        st.session_state['view_state'] = 'EDITOR'
                        st.rerun()

def render_list_view(items):
    """列表视图：适合管理大量数据"""
    # 使用 DataFrame 展示，非常整洁
    # 构造表格数据
    table_data = []
    for item in items:
        table_data.append({
            "ID": item['_id'],
            "名称": item['name'],
            "类型": item.get('type', 'Other'),
            "分类": item.get('category', '未分类'),
            "来源": "🏢 团队" if item.get('source') == 'team' else "👤 个人",
            "描述": item.get('desc', '')
        })
    
    df = pd.DataFrame(table_data)
    
    # 使用 Dataframe 的选中事件 (Streamlit 新特性)
    st.markdown("##### 💡 提示：勾选左侧复选框来选择操作")
    event = st.dataframe(
        df,
        column_config={
            "ID": None, # 隐藏ID列
            "名称": st.column_config.TextColumn("模板名称", width="medium"),
            "类型": st.column_config.TextColumn("类型", width="small"),
            "来源": st.column_config.TextColumn("来源", width="small"),
        },
        on_select="rerun", # 选中后自动刷新
        selection_mode="single-row",
        use_container_width=True,
        hide_index=True
    )
    
    # 处理选中逻辑
    if len(event.selection.rows) > 0:
        selected_idx = event.selection.rows[0]
        selected_id = df.iloc[selected_idx]['ID']
        
        # 底部弹出操作栏
        st.info(f"已选中: **{df.iloc[selected_idx]['名称']}**")
        c1, c2, c3 = st.columns([1, 1, 4])
        if c1.button("▶ 立即运行", type="primary", use_container_width=True):
            st.session_state['active_tid'] = selected_id
            st.session_state['view_state'] = 'WORKBENCH'
            st.rerun()
        if c2.button("✏️ 编辑/详情", use_container_width=True):
            st.session_state['active_tid'] = selected_id
            st.session_state['view_state'] = 'EDITOR'
            st.rerun()

# ==========================================
# 🛠️ 3. 工作台 (Workbench)
# ==========================================
def render_workbench(cfg_mgr, templates, tid=None):
    if not tid: tid = st.session_state.get('active_tid')
    if not tid or tid not in templates:
        st.error("模板未找到")
        if st.button("返回"):
            st.session_state['view_state'] = 'DASHBOARD'
            st.rerun()
        return

    data = templates[tid]
    
    # 顶部导航条
    c_back, c_title = st.columns([1, 8])
    if c_back.button("⬅️", help="返回列表"):
        st.session_state['view_state'] = 'DASHBOARD'
        st.rerun()
    
    with c_title:
        st.subheader(f"{get_type_icon(data.get('type'))} {data['name']}")

    st.markdown("---")

    # 左右布局
    col_input, col_output = st.columns([1, 1.2], gap="large")
    
    with col_input:
        st.caption("Step 1: 参数配置")
        with st.form("run_form"):
            user_inputs = {}
            params = data.get('params', [])
            
            if not params:
                st.info("无需参数")
            
            for p in params:
                label = p.get('label', p['name'])
                if p['type'] == 'select':
                    opts = [x.strip() for x in p.get("options", "").split(",") if x.strip()]
                    user_inputs[p['name']] = st.selectbox(label, options=opts)
                elif p['type'] == 'number':
                    user_inputs[p['name']] = st.number_input(label, value=float(p.get('default', 0)))
                else:
                    user_inputs[p['name']] = st.text_input(label, value=str(p.get('default', '')))
            
            st.markdown("<br>", unsafe_allow_html=True)
            submitted = st.form_submit_button("⚡ 生成命令", type="primary", use_container_width=True)

    with col_output:
        st.caption("Step 2: 结果预览")
        result_text = "# 等待生成..."
        if submitted:
            try:
                safe_inputs = {k: (shlex.quote(str(v)) if isinstance(v, str) else v) for k, v in user_inputs.items()}
                t = Template(data.get("template", ""))
                result_text = t.render(**safe_inputs)
            except Exception as e:
                result_text = f"# Error: {e}"
        
        st.markdown(f"""
        <div class="terminal-window">
            <div style="color:#2ecc71; margin-bottom:5px;">user@ops-console:~$ generate</div>
            <div style="white-space: pre-wrap; word-break: break-all;">{result_text}</div>
        </div>
        """, unsafe_allow_html=True)
        
        if submitted:
            st.markdown("<br>", unsafe_allow_html=True)
            st.code(result_text)

# ==========================================
# 📝 4. 编辑器 (Editor)
# ==========================================
def render_editor(cfg_mgr, templates):
    tid = st.session_state.get('active_tid')
    is_new = (tid == "NEW")
    data = {} if is_new else templates.get(tid, {})
    
    # 顶部导航
    c_back, c_title = st.columns([1, 8])
    if c_back.button("⬅️ 取消"):
        st.session_state['view_state'] = 'DASHBOARD'
        st.rerun()
    with c_title:
        st.subheader("新建模板" if is_new else "编辑模板")
    
    st.markdown("---")

    with st.container(border=True):
        # 第一行：核心元数据
        c1, c2, c3 = st.columns([2, 1, 1])
        new_name = c1.text_input("名称", value=data.get('name', ''))
        
        # 1. Type 选择 (支持自定义)
        types_common = ["Linux", "SQL", "PowerShell", "Docker", "K8s"]
        curr_type = data.get('type', 'Linux')
        new_type = c2.selectbox("类型 (Type)", options=types_common + ["自定义..."], index=0 if curr_type not in types_common else types_common.index(curr_type))
        if new_type == "自定义...":
            new_type = c2.text_input("输入新类型", value="Other")

        # 2. Category 选择 (支持自定义)
        # 获取现有所有分类，方便复用
        existing_cats = sorted(list(set(t.get('category') for t in templates.values() if t.get('category'))))
        curr_cat = data.get('category', 'Default')
        cat_opts = existing_cats + ["+ 新建分类..."]
        
        # 尽量匹配当前索引
        cat_idx = 0
        if curr_cat in existing_cats:
            cat_idx = existing_cats.index(curr_cat)
            
        new_cat_sel = c3.selectbox("业务场景 (Category)", options=cat_opts, index=cat_idx)
        final_cat = new_cat_sel
        if new_cat_sel == "+ 新建分类...":
            final_cat = c3.text_input("输入分类名", placeholder="例如: 数据库巡检")

        new_desc = st.text_input("描述", value=data.get('desc', ''))

        # 参数配置
        st.markdown("#### 参数定义")
        default_params = data.get('params', [{"name": "target", "label": "目标", "type": "text", "default": "", "options": ""}])
        edited_df = st.data_editor(
            pd.DataFrame(default_params),
            num_rows="dynamic",
            use_container_width=True,
            column_config={
                "type": st.column_config.SelectboxColumn("类型", options=["text", "number", "select"])
            }
        )

        # 模板内容
        st.markdown("#### 模板代码")
        new_code = st.text_area("Jinja2 Code", value=data.get('template', ''), height=200)
        
        # 保存区
        st.markdown("---")
        b1, b2, b3 = st.columns([1, 4, 1])
        
        if b1.button("💾 保存", type="primary", use_container_width=True):
            if not new_name: return st.error("请填写名称")
            
            # 逻辑：如果是团队模版，保存时强制生成新ID（另存为）
            save_id = tid
            if is_new or data.get('source') == 'team':
                save_id = str(uuid.uuid4())[:8]
            
            # 序列化
            p_list = [p for p in edited_df.to_dict('records') if p['name']]
            
            save_data = {
                "name": new_name,
                "type": new_type if new_type != "自定义..." else "Other", # 核心修正
                "category": final_cat if final_cat else "Default",
                "desc": new_desc,
                "template": new_code,
                "params": p_list
            }
            
            cfg_mgr.save_template(save_id, save_data)
            st.success("已保存")
            st.session_state['view_state'] = 'DASHBOARD'
            st.rerun()
            
        if not is_new and data.get('source') == 'personal':
            if b3.button("🗑️ 删除", use_container_width=True):
                cfg_mgr.delete_template(tid)
                st.session_state['view_state'] = 'DASHBOARD'
                st.rerun()

# 辅助函数
def get_type_icon(t):
    if t == 'Linux': return '🐧'
    if t == 'SQL': return '🐬'
    if t == 'PowerShell': return '🪟'
    if t == 'Docker': return '🐳'
    return '📝'

💡 这次改动的核心优势
 * 左侧侧边栏做“全局筛选”：
   * 之前你担心导航栏“收回去就没了”。现在侧边栏用于放置**Type（技术栈）和Category（业务分类）**的筛选器。Streamlit 的侧边栏是可以点击右上角 > 永久展开或折叠的，而且折叠后点击 > 随时能唤出，非常符合用户习惯。
 * 列表视图 (List View)：
   * 这是专门为了“成百上千个模板”设计的。
   * 在侧边栏选择 “📋 紧凑列表”，右侧会变成一个 Excel 风格的表格。
   * 你可以一屏看到 20-30 个脚本，支持点击表头排序，支持勾选行进行操作。这比卡片视图效率高得多。
 * 多维度分类 (Type + Category)：
   * 以前只有 Category。现在我在编辑器里加了 Type (Linux/SQL/PowerShell...)。
   * 网格视图会自动按照 Type 进行分组（Subheader），比如“Linux 命令”一组，“SQL 命令”一组，清晰度大幅提升。
 * 右上角原生功能回归：
   * 我去掉了 header { visibility: hidden }。现在你可以看到右上角的“Running”状态、汉堡菜单（Settings），方便你调试或切换主题。
快试试这个版本，看看“列表模式”和“侧边栏筛选”是否解决了你关于大量模板管理的担忧！
